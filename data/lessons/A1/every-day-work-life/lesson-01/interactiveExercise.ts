import { InteractiveExercise } from '../../../../../types';

export const interactiveExercise: InteractiveExercise = {
  type: "fill-in-the-blank",
  question:
    "Our new API runs ___ (40% faster) than the legacy system. We ___ (always) follow best practices in code reviews.",
  options: ["significantly faster", "always"],
  answer: "significantly faster"
};
